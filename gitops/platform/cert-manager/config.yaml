# Read by the platform ApplicationSet (and the CI render check).
component: cert-manager
sync: automated
namespace: cert-manager
chart:
  repoURL: https://charts.jetstack.io
  name: cert-manager
  version: v1.21.2
