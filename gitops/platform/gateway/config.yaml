# Local chart in this repository: the platform's single entry point (ADR 0035).
component: gateway
# Gateway API resources are defaulted by the API server; compare server-side (ADR 0036).
diff: server-side
sync: automated
namespace: envoy-gateway-system
chart:
  path: gitops/platform/gateway/chart
