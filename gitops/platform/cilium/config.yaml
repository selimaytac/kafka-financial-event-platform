# Read by the platform ApplicationSet and by the bootstrap stack (single source of truth).
component: cilium
namespace: kube-system
chart:
  repoURL: https://helm.cilium.io
  name: cilium
  version: 1.20.2
